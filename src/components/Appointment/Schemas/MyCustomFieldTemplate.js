import React from 'react';

export default function MyCustomFIeldTemplate(props) {
    const {id, classNames, label, help, required, description, errors, children} = props;
    return (
            <div>Test</div>
    );

}